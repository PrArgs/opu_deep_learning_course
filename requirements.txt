# Requirements for running mmn13.ipynb
# Install with: pip install -r requirements.txt
# NOTE: For torch, pick the appropriate wheel for your platform (CPU vs CUDA).
# The constraint below is generic; if you need CUDA support, follow instructions on https://pytorch.org

pandas>=1.5.0
numpy>=1.24.0
matplotlib>=3.5.0
seaborn>=0.12.0
torch>=1.13.0
ipykernel>=6.0.0
notebook>=6.4.0

# Optional / helpful (not strictly required by the notebook but commonly useful)
scikit-learn>=1.0.0
typing-extensions>=4.0.0
